Feature: Gestión de Tareas
============================
Administración de tareas asociadas a proyectos:
- components/  → TaskBoard (Kanban), TaskList, TaskForm, TaskCard
- hooks/       → useTasks, useTaskBoard
- services/    → taskService.ts
- types/       → Task, TaskStatus, TaskPriority
